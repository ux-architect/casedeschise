import { tourFields } from "./objects/event-fields";

const eventValcea = {
  name: "events-valcea",
  title: "Evenimente Valcea",
  type: "document",

 fields: [
    ...tourFields,
  ]
};

export default eventValcea;
